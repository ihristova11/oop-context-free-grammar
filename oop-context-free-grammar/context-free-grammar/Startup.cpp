#include <iostream>
#include <vector>
#include <string>

#include "Engine.h"

int main()
{
	Engine::getInstance().start();

	// seed grammars
	// read grammars from file (serialize them) -  override toString();




	return 0;
}